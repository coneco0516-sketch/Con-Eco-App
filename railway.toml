[build]
builder = "nixpacks"
buildCommand = "npm install && cd Frontend && npm install && npm run build && cd ../Backend && pip install -r requirements.txt"

[deploy]
startCommand = "cd Backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
